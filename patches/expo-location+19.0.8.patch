diff --git a/node_modules/expo-location/android/src/main/java/expo/modules/location/LocationModule.kt b/node_modules/expo-location/android/src/main/java/expo/modules/location/LocationModule.kt
index 46e2ec5..94986a8 100644
--- a/node_modules/expo-location/android/src/main/java/expo/modules/location/LocationModule.kt
+++ b/node_modules/expo-location/android/src/main/java/expo/modules/location/LocationModule.kt
@@ -272,6 +272,16 @@ class LocationModule : Module(), LifecycleEventListener, SensorEventListener, Ac
       return@AsyncFunction
     }
 
+    // Updates the foreground-service notification's title/body in place for
+    // an already-running location task, without re-registering the task or
+    // touching the location subscription (unlike calling
+    // startLocationUpdatesAsync again, which re-runs permission checks and
+    // task registration — too heavy to call on every location update).
+    AsyncFunction("updateForegroundServiceNotificationAsync") { taskName: String, title: String, body: String ->
+      LocationTaskConsumer.getInstance(taskName)?.updateNotification(title, body)
+      return@AsyncFunction
+    }
+
     AsyncFunction("hasStartedLocationUpdatesAsync") { taskName: String ->
       return@AsyncFunction mTaskManager.taskHasConsumerOfClass(taskName, LocationTaskConsumer::class.java)
     }
diff --git a/node_modules/expo-location/android/src/main/java/expo/modules/location/services/LocationTaskService.kt b/node_modules/expo-location/android/src/main/java/expo/modules/location/services/LocationTaskService.kt
index 80647ab..7a45691 100644
--- a/node_modules/expo-location/android/src/main/java/expo/modules/location/services/LocationTaskService.kt
+++ b/node_modules/expo-location/android/src/main/java/expo/modules/location/services/LocationTaskService.kt
@@ -89,6 +89,7 @@ class LocationTaskService : Service() {
     }
 
     return builder.setCategory(Notification.CATEGORY_SERVICE)
+      .setOngoing(true)
       .setSmallIcon(applicationInfo.icon)
       .build()
   }
diff --git a/node_modules/expo-location/android/src/main/java/expo/modules/location/taskConsumers/LocationTaskConsumer.kt b/node_modules/expo-location/android/src/main/java/expo/modules/location/taskConsumers/LocationTaskConsumer.kt
index e659285..55a33e3 100644
--- a/node_modules/expo-location/android/src/main/java/expo/modules/location/taskConsumers/LocationTaskConsumer.kt
+++ b/node_modules/expo-location/android/src/main/java/expo/modules/location/taskConsumers/LocationTaskConsumer.kt
@@ -55,11 +55,13 @@ class LocationTaskConsumer(context: Context, taskManagerUtils: TaskManagerUtilsI
 
   override fun didRegister(task: TaskInterface) {
     mTask = task
+    instances[task.name] = this
     startLocationUpdates()
     maybeStartForegroundService()
   }
 
   override fun didUnregister() {
+    mTask?.let { instances.remove(it.name) }
     stopLocationUpdates()
     stopForegroundService()
     mTask = null
@@ -227,6 +229,17 @@ class LocationTaskConsumer(context: Context, taskManagerUtils: TaskManagerUtilsI
     mService?.stop()
   }
 
+  // Rebuilds and re-posts the foreground notification with new text, reusing
+  // the same startForeground(bundle) call the "already running" branch of
+  // maybeStartForegroundService() uses above — this does not touch the
+  // location subscription at all, so it's safe to call frequently.
+  fun updateNotification(title: String, body: String) {
+    val bundle = Bundle()
+    bundle.putString("notificationTitle", title)
+    bundle.putString("notificationBody", body)
+    mService?.startForeground(bundle)
+  }
+
   private fun deferLocations(locations: List<Location>) {
     val size = mDeferredLocations.size
     var lastLocation = if (size > 0) mDeferredLocations[size - 1] else mLastReportedLocation
@@ -318,5 +331,12 @@ class LocationTaskConsumer(context: Context, taskManagerUtils: TaskManagerUtilsI
     fun shouldUseForegroundService(options: Map<String?, Any?>): Boolean {
       return options.containsKey(FOREGROUND_SERVICE_KEY)
     }
+
+    // Registry of running consumers by task name, so LocationModule can reach
+    // a specific instance to update its notification without a generic
+    // lookup path existing on TaskManagerInterface.
+    private val instances = mutableMapOf<String, LocationTaskConsumer>()
+
+    fun getInstance(taskName: String): LocationTaskConsumer? = instances[taskName]
   }
 }
