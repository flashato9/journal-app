Instead of Retry Connection as a separate button there should be a companion settings section with a retry connection button. When pressed the connection is retried. (Done)

Remove the debug LLM option. It should no longer be an option. Just comment it out. But don't remove the page for now. (Done)
